# Kernel Panic
The kernel panic happens when:

- The 0xID8 emulator is launched without specifying a ROM to load.
- The progam counter (PC) of the 0xID8 emulator overflows the memory address limit (0xFFF)
